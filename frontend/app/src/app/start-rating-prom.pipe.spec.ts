import { StartRatingPromPipe } from './start-rating-prom.pipe';

describe('StartRatingPromPipe', () => {
  it('create an instance', () => {
    const pipe = new StartRatingPromPipe();
    expect(pipe).toBeTruthy();
  });
});
